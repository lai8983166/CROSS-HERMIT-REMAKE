# Tasks: add-unit-sprites

## 1. 逆向定案（先容器后像素）

- [x] 1.1 容器级：01E.BIN 头/块偏移表解析（末块终点==总长校验）、块头结构画像
       （帧数/宽高字段候选、与 9 块的关系）。验证：分析脚本输出 + 结论回写本 design
- [x] 1.2 像素级假设族：8bpp+UNITPAL 直读 / 行程编码 / 分块 三假设各自渲染样帧，
       视觉+直方图裁决；调色板绑定字段定位。验证：样帧 PNG 目检可辨认单位轮廓
       ——实裁定：帧=标准 8bpp BMP 内嵌调色板（无需 UNITPAL）；单位本体在 {A..E}{0,1}A.BIN，
       ##E 全为特效（详见 design.md 逆向定案节）

## 2. 导出管线

- [x] 2.1 tools/unit_anim_export.py：容器→assets/unit/*.png + data/unit_sprites.json
       （帧表含尺寸/锚点/朝向映射/调色板 id/_meta.open_items）。验证：全 10 单位档导出无错、
       帧数与容器一致（01E 为特效档已改导 {A..E}{0,1}A）
- [x] 2.2 formats.md 新 §（DxAnim 格式定案/开口分明）。验证：§10 已写，开口逐项列明

## 3. sim 集成

- [ ] 3.1 BattleUnit 增朝向字段（移动向量 8 向量化）+ main.gd 精灵绘制分支
       （帧表驱动、锚点对格心、脚底阴影）。验证：窗口运行精灵随状态/朝向换帧
- [ ] 3.2 回退与配置：battle_setup.json 可指定 anim_id；资产缺失回退色块+HUD 提示。
       验证：删 assets/unit 后运行功能不损

## 4. 回归与验收

- [ ] 4.1 测试：帧表加载/朝向量化/序列选择/回退 4 组用例 + 全量 runner 绿。
       验证：0 failures
- [ ] 4.2 用户目检：窗口中单位为原版精灵动画，与实机截图单位观感可对照。
       验证：用户确认
- [ ] 4.3 提交 + 归档（含分析结论文档）
