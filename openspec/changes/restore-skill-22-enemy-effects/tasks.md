## 1. 原版语义与黄金证据

- [x] 1.1 沿 `48B2E0 case 6 → 48BBE0 → 48B720/48C850` 及后续命中、抵抗和目标写入调用恢复玩法效果 6 的参数与字段语义，更新逆向文档，并以技能 22 表项和至少一个同分派条目交叉验证数值、持续时间及结果分支。
- [x] 1.2 追踪技能演出对 3027 的消费者，确认释放阶段的锚点、结束条件以及是否存在源目标插值、追踪或其他外部位移；用反编译地址和逐帧数据记录结论，证据不足时明确登记无移动语义而不猜测。
- [x] 1.3 核对原版动作选择表中 action 13、31、16 的八方向映射，并扫描十档单位资源选出三个动作均可解析、施法或释放阶段具有多帧变化且调色亮度合格的演示档，以黄金断言固定选择依据。

## 2. 动作与特效资产导出

- [x] 2.1 扩展单位动作导出集合以包含 action 31，重新生成 `unit_sprites.json`，并用 Python 测试验证技能 22 的 action 13/31/16 八方向动画、镜像、帧引用及既有 action 5、11～16 不回退。
- [x] 2.2 将 EFCT 2044、3027、2029 及全部递归依赖帧加入导出，重新生成特效数据和 PNG，并验证三个全局 ID、程序块映射、持续/循环信息、复合图层及所有帧引用合法。
- [x] 2.3 仅在任务 1.2 证明外部移动时为 3027 导出枚举式路径模式及参数；否则保留原始时间线和锚点，并用测试确保运行时不会自动套用线性、追踪或抛物线移动。

## 3. 玩法效果与技能阶段

- [ ] 3.1 为效果 ID 6 实现独立、可注入 RNG 的结算路径，只读写任务 1.1 已确认字段，并用成功、未命中或抵抗、持续时间和重复种子测试验证；不得回退到通用物理伤害。
- [ ] 3.2 扩展技能事件以记录玩法结果、应用值或状态参数及结算帧，同时保持 `gameplay_effect_id` 与 2044/3027/2029 视觉 ID 分离，并验证普通攻击与技能 29 的事件结构兼容。
- [ ] 3.3 接入技能 22 的 action 13/31/16 和 2044→3027→2029→恢复阶段，按任务 1.2 的证据处理释放锚点或路径，并用 Godot 离屏测试验证阶段顺序、每阶段只启动一次、从首帧播放及结束后无残留。
- [ ] 3.4 将数据驱动演示切换或扩展为技能 22 敌方样例，验证 `target_filter=2` 接受敌方并拒绝友方/自身，且配置使用任务 1.3 选定的可见多帧单位档。

## 4. 回归与目检

- [ ] 4.1 运行 Python 导出/解码测试、Godot 全量离屏测试与 `openspec validate restore-skill-22-enemy-effects --strict`，要求全部通过，并确认技能 29、普通攻击和既有伤害公式的黄金结果不变。
- [ ] 4.2 启动可见原型供人工目检技能 22：人物施法动作、持续施法、释放或投射、敌方命中特效、命中或抵抗结果、恢复及重复施放均与已确认原版数据一致。
