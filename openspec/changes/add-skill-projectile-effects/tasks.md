## 1. 原版数据与动画导出

- [x] 1.1 导出技能明细表的动作、`cast_fx`、`release_fx`、`sync_fx`、`impact_fx`、范围形状及独立的 `gameplay_effect_id`，并用技能 22/29 与普通攻击 101/103 黄金值测试验证。
- [x] 1.2 扩展 DxAnim 解释器，使 `EFCT.BIN` 的外部生命周期持续动画能产生可证明的 `loop_from`，并验证 2044～2051 可收敛且原有一次性动画、单位动画测试不回退。
- [x] 1.3 将特效导出器迁移到 `EFCT.BIN` 全局动画 ID，导出技能 29 依赖的 2050/2098/3017/3032 帧与时间线，并验证 2042、3027 的 block/animation 映射及所有帧引用合法。
- [x] 1.4 将单位动作导出推广到 action 11～16 的八方向映射，重新生成单位数据并验证动作、镜像、空动作回退以及既有 MOVE/ATTACK 黄金序列。
- [x] 1.5 更新 DxAnim 与技能演出逆向文档，记录 `EFCT.BIN`、技能明细字段、通用命中表和攻击表玩法效果分派的证据及仍未确认的移动弹道语义。

## 2. 技能事件与阶段运行时

- [x] 2.1 将战斗事件中的旧 `effect_id` 迁移为 `gameplay_effect_id` 和分阶段视觉 ID，验证普通攻击 101/103 仍不生成独立外置特效。
- [x] 2.2 实现固定逻辑帧驱动的技能施法阶段，支持持续施法终止、释放、空白同步等待、命中和恢复，并用离屏测试验证技能 29 的阶段顺序与 3017 的 120 tick 延迟。
- [x] 2.3 扩展单位状态与视图，使施法者朝向目标播放明细表指定的 action 12/15，空动作安全回退待机；用 Godot 测试验证连续施放均从首帧开始。
- [x] 2.4 将特效实例改为按 `global_id`、阶段、源/目标坐标和生命周期渲染，验证持续特效在释放时清除、命中特效在目标格播放且结束后无残留。
- [x] 2.5 增加技能 29 的数据驱动演示入口或战斗配置，不引入完整技能选择 UI，并验证无需修改代码即可切换样例技能或关闭演出。

## 3. 回归与目检

- [x] 3.1 运行 Python 导出/解码测试和 Godot 全量离屏测试，要求 0 failures，并严格校验 `add-skill-projectile-effects` OpenSpec 变更。
- [x] 3.2 启动可见原型供用户目检技能 29：施法动作与持续特效、释放阶段、120 tick 同步、目标格命中动画、恢复及重复施放均符合导出数据。
- [x] 3.3 根据攻击表 `target_filter=3` 修正技能 29 的友方/自身目标语义，拒绝敌方目标，并验证未还原玩法效果不会错误扣减友军生命。
- [x] 3.4 修正可见演示误用单帧 `A0A` 施法动作的问题，改用具有原版四帧 action 12 且亮度适合目检的 `E0A`，并以八方向多帧测试防止回退。
