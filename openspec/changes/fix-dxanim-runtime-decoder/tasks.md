## 1. 修正底层 DxAnim 解码

- [x] 1.1 在 `tools/dxanim_lib.py` 中实现 block 0 指令与 block 4 四个 12 位描述符的严格解析，保留最终图片编号、偏移、属性和持续时间，并为越界引用与截断数据给出明确错误；用 A0A/B1A 的移动动画 11–15 号黄金数据验证描述符到最终图片的映射。
- [x] 1.2 将 block 0–3 的控制流实现为有步数上限的离线解释器，覆盖等待、跳转、循环、终止以及 opcode 1 创建的 block 3 并发子动画；用复合动画夹具验证同一时刻能够产生多图层，异常循环会稳定失败而不是卡住。
- [x] 1.3 从原版动作/方向表提取移动动作编号 3 的八方向映射，将低两位作为播放/镜像标志传递，不再使用“动画号 +5”或把标志误作 block 号；验证八方向均能解析且直接落到 block 0 的 11–15 号动画。
- [x] 1.4 运行解码器单元测试和 A0A/B1A 黄金测试，只暂存本组相关文件并提交一个独立的小提交。

## 2. 升级导出数据与资源

- [ ] 2.1 扩展单位动画导出格式，使每个时间片可包含有稳定绘制顺序的 `layers`、逐层偏移/镜像属性、持续时间和 `loop_from`，同时保留旧单帧记录的读取兼容；重新导出当前单位资源并验证每个单位都有八方向移动时间线、所有图片引用均在范围内。
- [ ] 2.2 让攻击特效导出复用修正后的解释器并重新导出特效资源；用已知斩击/法球样本核对最终图片序列、持续时间和复合层并发关系。
- [ ] 2.3 更新 `docs/units/` 与相关资源说明，记录动作编号、方向表、描述符链路、复合动画语义和新旧 JSON 格式；运行文档中列出的复现命令，确保路径和输出与当前项目一致。
- [ ] 2.4 运行单位与特效导出验证，只暂存本组脚本、生成数据、测试和文档并提交一个独立的小提交。

## 3. 修正 Godot 实机播放

- [ ] 3.1 在 `main.gd` 的单位动画解析中支持新多图层时间线和旧单帧时间线；以 `(状态, 八方向, 时间线)` 作为播放身份，并在状态或方向改变时确定性重置动画起点。
- [ ] 3.2 按时间片绘制全部图层，逐层应用原始 x/y 偏移、水平镜像、绘制顺序和现有换色规则，不增加单位级全局补偿；验证单层旧数据的显示位置保持不变。
- [ ] 3.3 添加运行时测试，覆盖精确帧序列/时长、八方向切换后从首帧开始、逐层镜像与偏移、复合图层顺序以及缺失动作时的现有回退行为。
- [ ] 3.4 运行全部 Godot 测试，只暂存本组运行时代码和测试并提交一个独立的小提交。

## 4. 集成验证与人工对照

- [ ] 4.1 从干净导出流程重建单位和攻击特效数据，运行 Python 测试、资源完整性检查及全部 Godot 测试，记录测试数量与零失败结果。
- [ ] 4.2 制作 A0A/B1A 八方向移动对照截图或短录屏，逐方向对比原版描述符解析结果与实机播放，确认帧序列、速度、镜像、偏移和循环起点一致。
- [ ] 4.3 复查活动中的 `add-attack-effects` 变更，确认解码器修正未破坏其测试；只有在用户完成视觉确认后，才更新该变更中对应的人工验收任务。
- [ ] 4.4 对最终差异做精确暂存检查，确保未混入工作区既有的行尾变化，并提交集成验证的小提交。
