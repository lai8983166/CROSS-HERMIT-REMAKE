# Tasks: add-map-composition

## 1. 结构分析（design 决策 6 先行）

- [x] 1.1 两表画像：255×4B 索引表（值域/三元组分布/与 12 个 terrain 值的关系）+
       53×(u16,u16) 步长表（单调性/差分/与页几何 256/128/64 的关系）→ 结论回写本 design。
       验证：分析脚本留痕（analysis/ 或临时输出进提交信息），结论可复述
- [x] 1.2 贴片栅格探测：图集页内边缘响应自相关 → 贴片尺寸/相位（或明确失败 → 用 32×16 假设）。
       验证：探测结果 + 一页的可视化标注图

## 2. 映射与合成

- [x] 2.1 假设族实现 (iso 8 朝向全灭→直角定案, 详见 design 决策 0)：compose_map.py 内 3~4 候选映射（可切换），各自合成临时图并与回退 BMP
       算相关度 + 输出对比报告。验证：报告含各假设得分与素材平铺基线
- [x] 2.2 定案映射（得分最高且超基线，平手人工裁决）→ 正式产出 assets/map01_composed.png(+json)。
       验证：相关度 ≥0.5 且 > 素材平铺基线
- [x] 2.3 三层语义收敛 → docs/formats.md §5 更新（定案/开口标注分明）。
       验证：formats.md 无"待定"含糊表述——每项要么定案要么显式开口

## 3. 接入与验收

- [x] 3.1 main.gd 贴图态换 composed 图；叠加态按 world bbox 直贴（无拉伸）。
       验证：窗口 `--quit-after 300` 0 错误（前台）；T 循环 = 合成图/叠加/数据
- [x] 3.2 回归：全量测试绿 + assets/README 更新。
       验证：runner 0 failures
- [ ] 3.3 用户目检：合成图宏观布局与回退 BMP 对应（可辨认道路/建筑群）、叠加态逐格对齐、
       战斗照常。验证：用户确认
- [ ] 3.4 提交（含 composed PNG/JSON 与分析结论）
